---
- name: "Engine Failure: Stop the Warpdrive Engine (VM Shutdown)"
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    aws_region: "{{ lookup('env', 'AWS_REGION') | default('eu-west-1', true) }}"
    instance_id: "{{ lookup('env', 'WARPDRIVE_ENGINE_INSTANCE_ID') }}"
    scui_host: "{{ lookup('env', 'SCUI_HOST') | default('localhost:8080', true) }}"
    scui_protocol: "{{ lookup('env', 'SCUI_PROTOCOL') | default('http', true) }}"
    api_token: "{{ lookup('env', 'AAP_API_CLIENT_BEARER_TOKEN') | default('', true) }}"

  tasks:
    - name: Validate instance ID is set
      ansible.builtin.assert:
        that:
          - instance_id | length > 0
        fail_msg: >
          WARPDRIVE_ENGINE_INSTANCE_ID is not set.
          Run 'cd terraform && terraform output warpdrive_engine_instance_id'
          and add it to your .env file.

    - name: Trigger SCUI crisis state
      ansible.builtin.uri:
        url: "{{ scui_protocol }}://{{ scui_host }}/api/controller/ui/home"
        method: POST
        body_format: json
        body:
          scenario: engine_failure_init
        headers: >-
          {{
            (api_token | length > 0)
            | ternary(
                {'Authorization': 'Bearer ' + api_token, 'Content-Type': 'application/json'},
                {'Content-Type': 'application/json'}
              )
          }}
        status_code: [200, 204]
      ignore_errors: true

    - name: Stop the Warpdrive Engine instance
      amazon.aws.ec2_instance:
        region: "{{ aws_region }}"
        instance_ids:
          - "{{ instance_id }}"
        state: stopped
        wait: true
        wait_timeout: 120
      register: stop_result

    - name: Report
      ansible.builtin.debug:
        msg: >-
          Warpdrive Engine {{ instance_id }} stopped.
          CloudTrail -> EventBridge -> SQS -> EDA pipeline will detect this
          and trigger automatic remediation.
